# Sibling module pins for CI builds — same contract as isdr-app/siblings.lock:
# settings.gradle.kts composes ../isdr-proto; the release workflow clones it
# at this exact commit so a tag build is reproducible. Bump in the same
# commit that starts depending on a sibling change.
# Format: <repo-name>=<commit-sha>, cloned from github.com/nrolabs/<repo-name>.
isdr-proto=232abdccc7e4d49a1f59ad1bb48fdb6ebcf314c0
